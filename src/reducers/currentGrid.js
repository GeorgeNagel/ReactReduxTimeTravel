const initialState = [
  [null, null, null],
  [null, null, null],
  [null, null, null]
]
const currentGrid = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default currentGrid